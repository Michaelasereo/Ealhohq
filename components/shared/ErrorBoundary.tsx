"use client";

import type { ReactNode } from "react";

/** Placeholder — replace with a proper React error boundary when wiring Sentry. */
export function ErrorBoundary({ children }: { children: ReactNode }) {
  return children;
}
